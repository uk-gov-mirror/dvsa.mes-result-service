import { StandardCarTestCATBSchema } from '@dvsa/mes-test-schema/categories/B';

export const saveTestResult = (testResult: StandardCarTestCATBSchema): Promise<void> => {
  return Promise.resolve();
};
