service: mes-microservices-starter-template

provider:
  name: aws
  runtime: nodejs8.10

functions:
  postResult:
    handler: src/functions/postResult/framework/handler.handler
    events:
      - http:
          path: test-result
          method: post
  
  getNextUpdateBatch:
    handler: src/functions/getNextUpdateBatch/framework/handler.handler
    events:
      - http:
          path: test-result/upload?interface={interface}&batch_size={batch_size}
          method: get

custom:
  dynamodb:
    start:
      port: 8000
      inMemory: true
      migrate: true
  webpack:
    webpackConfig: 'webpack-sls-offline.config.js'

plugins:
  - serverless-webpack
  - serverless-offline
