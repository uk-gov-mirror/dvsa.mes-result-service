/**
 * Returns a batch of Tars records ordered by oldest data and limited to the number
 * specified, where the upload status is currently unprocessed.
 *
 * @param batchSize: number
 */
export const buildTarsNextBatchQuery = (batchSize: number): string => {
  return '';
};
